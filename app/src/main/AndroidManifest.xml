<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ludio.zetbio">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <!--Login View-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!--Splash-->

        <!--Information List-->
        <activity android:name=".MainViewActivity" />

        <!--SignUp Page-->
        <activity android:name=".SignUpActivity" />

        <activity android:name=".PsearchActivity" />

        <activity android:name=".SetUpActivity" />

        <activity android:name=".NewPatientRegister" />

        <activity android:name=".FindIdPasswordActivity" />

        <activity android:name=".Earthinformation" />

        <activity android:name=".CountryInformationActivity" />

    </application>

</manifest>